const Gradient = ({ className }: { className?: string }) => {
  return (
    <div
      className={`${className} max-h-[544px] max-w-[544px] bg-gg blur-[175px]`}
    ></div>
  );
};

export default Gradient;
